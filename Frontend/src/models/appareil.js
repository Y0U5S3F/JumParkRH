class Appareil {
    constructor(id, nom, ip, port, created_at, status) {
      this.id = id;
      this.nom = nom;
      this.ip = ip;
      this.port = port;
      this.created_at = created_at;
      this.status = status;
    }
  }
  
  export default Appareil;
  